# src/config.py
import os

BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

# REMOTE (Acer Laptop via SMB)
CALIBRE_PATH = "/Volumes/Calibre2" 
DB_FILE = os.path.join(CALIBRE_PATH, "metadata.db")

# LOCAL (M1 SSD)
CHROMA_PATH = os.path.join(BASE_DIR, "chroma_db")
EMBED_MODEL = "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2" #"sentence-transformers/all-MiniLM-L6-v2" 

# CHUNK SETTINGS
CHUNK_SIZE = 1500 # для тех/книг мало 700 символов.
CHUNK_OVERLAP = 150 # 70

# Mapping of Calibre Tags to RAG Categories
CATEGORY_HIERARCHY = {
    "LLM set": ["LLM"],
    "NLP set": ["NLP", "LLM"],
    "DL set":  ["DL", "NLP", "LLM"],
    "ML set":  ["ML", "DL", "NLP", "LLM"],
    "AI set":  ["AI", "ML", "DL", "NLP", "LLM"]
}

# Иерархия от частного к общему (чем левее, тем строже)
STRICT_ORDER = ["LLM", "NLP", "DL", "ML", "AI"]

def get_best_category_set(calibre_tags_str: str) -> str:
    """
    Выбирает самый строгий тег из доступных.
    Пример: 'AI, IT, NLP, Java' -> 'NLP set'
    """
    if not calibre_tags_str:
        return "AI set"
        
    # Чистим теги из Calibre
    tags = [t.strip().upper() for t in calibre_tags_str.split(",")]
    
    # Ищем первое совпадение в порядке строгости
    for s_tag in STRICT_ORDER:
        if s_tag in tags:
            return f"{s_tag} set"
            
    return "AI set" # Дефолт, если совпадений нет

# в этом проекте отсутствуют спец/токены (как в chatbot), т.к. ollama позаботится об этом

# RAG_PROMPT_BASIC = "Проанализируй контекст и кратко ответь на вопрос пользователя на русском языке."
# RAG_PROMPT_BASIC = "Кратко ответь на вопрос на русском языке, основываясь на контексте."
# RAG_PROMPT_BASIC = "Проанализируй предоставленный КОНТЕКСТ. Перечисли ВСЕ названия книг, которые там упоминаются, кроме уже названных."

# RAG_PROMPT_COT = "Сначала проведи технический анализ в <thought>, затем дай итоговый ответ. Пиши только на русском."


# RAG_PROMPT_COT = """
# ### СТРОГИЙ ТЕХНИЧЕСКИЙ АУДИТОР ###
# Твоя главная задача: проверить наличие фактов в КОНТЕКСТЕ. 

# ПРАВИЛА:
# 1. В блоке <thought> начни с фразы: "Проверяю наличие [название термина] в предоставленном тексте..."
# 2. Если в КОНТЕКСТЕ нет упоминания конкретного алгоритма или автора, о которых спрашивает юзер — ТЫ ОБЯЗАН СКАЗАТЬ: "В предоставленных материалах информация об этом отсутствует".
# 3. ЗАПРЕЩЕНО выдумывать или использовать внешние знания, если их нет в тексте.

# СТРУКТУРА:
# <thought>
# (Анализ: есть ли этот термин в тексте? На каких страницах/абзацах? Если нет — почему?)
# </thought>

# ИТОГОВЫЙ ОТВЕТ:
# (Только факты из текста или признание отсутствия информации)
# """

# RAG_PROMPT_COT = """
# ### ТЕХНИЧЕСКИЙ АНАЛИТИК (DEEP THINK) ###
# 1. Проанализируй КОНТЕКСТ на наличие информации по теме вопроса.
# 2. Если в тексте обсуждаются компоненты темы (например, Трансформеры, Слои, Веса), используй их для объяснения.
# 3. В блоке <thought> опиши, какие ТЕХНИЧЕСКИЕ СВЯЗИ ты нашел в разных книгах.
# 4. Если информация ДЕЙСТВИТЕЛЬНО отсутствует (даже косвенно), только тогда скажи "Не нашел".

# КОНТЕКСТ: {context}
# ВОПРОС: {question}
# """

# RAG_PROMPT_COT = """
# ### ИНСТРУКЦИЯ DEEP THINK ###
# 1. Сначала проанализируй вопрос и контекст в блоке <thought>.
# 2. Опиши логику рассуждения на русском языке.
# 3. Затем составь подробный итоговый ответ на русском языке.
# ОБЯЗАТЕЛЬНО начни ответ с тега <thought>.
# """

# RAG_PROMPT_COT = """
# ### ИНСТРУКЦИЯ DEEP THINK ###
# 1. Проанализируй контекст и вопрос.
# 2. Опиши логику в блоке <thought>.
# 3. ОБЯЗАТЕЛЬНО ЗАКРОЙ БЛОК ТЕГОМ </thought>.
# 4. Напиши заголовок ### ИТОГОВЫЙ ОТВЕТ ### и сам ответ.
# """

# RAG_PROMPT_COT = """
# ### ИНСТРУКЦИЯ: ГЛУБОКИЙ АНАЛИЗ ###
# ТЫ ОБЯЗАН ПИСАТЬ ВЕСЬ ОТВЕТ (И РАССУЖДЕНИЕ, И ИТОГ) ТОЛЬКО НА РУССКОМ ЯЗЫКЕ.

# ПЛАН ОТВЕТА:
# 1. В блоке <thought> проведи технический анализ контекста НА РУССКОМ.
# 2. Обязательно закрой блок тегом </thought>.
# 3. Напиши заголовок ### ИТОГОВЫЙ ОТВЕТ ### и выведи результат НА РУССКОМ.
# """
# RAG_PROMPT_COT = """Отвечай строго по плану: 
# 1. Начни с блока <thought>, где на русском проанализируй контекст и логику. 
# 2. Закрой блок тегом </thought>. 
# 3. Напиши '### ИТОГОВЫЙ ОТВЕТ ###' и дай ответ на русском."""

# RAG_PROMPT_COT = """
# ### ИНСТРУКЦИЯ: ГЛУБОКИЙ АНАЛИЗ ИСТОЧНИКОВ ###
# 1. В <thought> проанализируй список источников из предоставленного контекста.
# 2. Сравни их с историей чата: есть ли там НОВЫЕ книги, которые мы еще не обсуждали?
# 3. В итоговом ответе перечисли эти книги или подтверди, что в библиотеке только одна книга этого автора.
# ОТВЕЧАЙ ТОЛЬКО НА РУССКОМ.
# """


RAG_PROMPT_COT = """ОБЯЗАТЕЛЬНО используй формат: 
<thought> 
Твое пошаговое рассуждение на русском языке.
</thought>. 
### ИТОГОВЫЙ ОТВЕТ ###
Твой финальный ответ на русском языке."""

RAG_PROMPT_BASIC = "Ты — лаконичный технический эксперт. Отвечай на русском языке, используя ТОЛЬКО предоставленный КОНТЕКСТ. Если информации нет, скажи об этом."


LLM_CONFIG = {
    "model": "llama3",
    "temperature": 0.4,
    "repeat_penalty": 1.2,
    "top_p": 0.5,
    "num_predict": 800, # для CoT 300 будет мало
    "num_ctx": 4096,
}